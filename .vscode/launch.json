{
  "version": "0.2.0",
  "configurations": [
    {
      "cwd": "${workspaceFolder}/rest-api",
      "type": "node",
      "request": "launch",
      "name": "REST API",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "program": "${workspaceFolder}/rest-api/servidor.js"
    }
  ]
}
